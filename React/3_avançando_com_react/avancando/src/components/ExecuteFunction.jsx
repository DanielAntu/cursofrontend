import React from "react";

const ExecuteFunction = ({ myFunction }) => {
    return (
        <div>
            <button onClick={myFunction}>Clique em min</button>
        </div>
    );
};

export default ExecuteFunction;
